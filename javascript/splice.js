/*The JavaScript array splice()
 method is used to add/remove the elements to/from the existing array. 
 It returns the removed elements from an array. The splice() method also
 modifies the original array.
 */

 var arr=[1,2,3,4,5];
 var result=arr.splice(3);
 console.log(result);